# Transformer Attention + FFN Co-Sparse Accelerator — 機制與硬體規格 (v5)

**Target**: Xilinx KV260 FPGA (117K LUT, 1248 DSP, 4GB DDR4, 640 KB BRAM)
**Scope**: LLaMA-family decoder-only Transformer (TinyLlama, Qwen, LLaMA2/3, Mistral, ...)

**v5 相對 v4 的機制修正 (重要)**:
- Stage 2 由 **Block dispatch** 改為 **Global Top-N selection** (row-global, 非 block 配額)
- SIGN 補償簡化為 `β × Σ q_d·sign(k_d)` (移除 mean(|K_drop|) 縮放)
- K-anchor 改為 **per-position streaming running-max** (零 offline 零額外存儲)
- 新增 **FFN activation sparsity** (attention + FFN 雙稀疏, 共用 sparse gather)
- 移除 mean(|K_drop|) table、block FSM、margin/overflow 邏輯

---

## 0. 系統總覽

兩個正交稀疏機制：

| 區塊 | 機制 | selection | 省的資源 |
|------|------|-----------|---------|
| Attention (Q·K·V) | RAD-Cascade + KIVI-V | token-level (active dim + Global Top-N) | KV cache BW + MAC |
| FFN (SwiGLU) | Activation sparsity | neuron-level (gate proxy) | FFN 權重 BW (W_up/W_down) |

兩機制統計獨立 (整合實測交互項 ≈ +0.09 pp，近線性疊加)，共用 Sparse Gather Engine。

---

## 1. 機制 (model-agnostic)

### Notation

| Symbol | 定義 |
|--------|------|
| `L` | 層數 |
| `H_Q` / `H_KV` | Query / KV head 數 (GQA) |
| `D` | head_dim |
| `P` | num_pairs = D / 2 |
| `S` | sequence length |
| `A` | active pairs per query |
| `n_K` | K-anchor pair 數 |
| `N` | Global Top-N candidate 數 |
| `G` | KIVI V group size |
| `d_ff` | FFN intermediate size |
| `K_ffn` | FFN neuron 剪枝率 |

### Layer-wise dispatch

| Layer | Attention | FFN |
|-------|-----------|-----|
| Layer 0 | 異質: 跑滿 D dim (active=P, n_K=0), 8-bit Q·K + 8-bit V, 不套 RAD | activation sparsity |
| Layer 1 ~ L-1 | RAD-Cascade (active + SIGN + Global Top-N) + KIVI-V | activation sparsity |

Layer 0 異質原因: per-row attention 分散度高 (max ≈ 1/S), RAD selection 不適用; 跑滿維度, 後續層才省。

---

### 1.A Attention: RAD-Cascade

量化: Q, K per-token INT8。

#### Stage 1: Active path + SIGN 補償 (Q·K 端)

**Active dim 選擇** (8-bit MAC, full-64-dim einsum with mask):
- 每 query 選 `A` 個 active pair (default `A = round(0.375 × P)`)，兩類組成：
  - `n_K` 個 **K-anchor pair**: per-position streaming running-max of |K| 選 top-n_K (見 2.7)
  - `A - n_K` 個 **dynamic pair**: per-query |Q| pair-mag (INT8 L1) 選 top, anchor scatter 排除避免重複
- pair → dim: `full_dim_mask = pair_mask.repeat_interleave(2)` (consecutive pair (2j,2j+1))
- `active_score = einsum(Q_b ⊙ full_dim_mask, K_b)` (mask 乘法後全 64 維 einsum, 等價只算 active 維但避免手動 index)
- Q-anchor 廢除 (n_Q = 0, 見 2.5)

**SIGN 補償** (1-bit sign + 8-bit |Q| 累加):
```
sign_k = (K_b >= 0) ? +1 : -1
comp   = einsum(Q_b ⊙ dropped_mask, sign_k)    # Σ_d∈dropped  q_d · sign(k_d)
stage1 = active_score + β × comp
```
- β = 1.0 (Theorem 1)
- **無 mean(|K_drop|) 縮放** (v4 的縮放版廢除, ground-truth 為純 Σ q·sign(k))

**Theorem 1** (SIGN unbiased): 對稱分布下 E[sign(k)·k] = mean(|k|), β=1.0 為 unbiased estimator。

#### Stage 2: Global Top-N selection (取代 Block dispatch)

```
full_score    = einsum(Q_b, K_b)                       # 全 64 維 INT8 (候選用)
stage1_masked = where(valid_causal, stage1, -inf)
topN_idx      = topk(stage1_masked, N)                  # row-global top-N
cand_mask     = scatter(topN_idx) & valid_causal
final_score   = where(cand_mask, full_score, stage1)    # 候選用 full, 非候選保留 stage1 近似
```

- **核心**: Top-N 選「誰用精算 full_score」, **非候選 token 保留 stage1 近似** (不丟棄/不 mask -inf)。尾巴 token 仍以近似權重參與 softmax, 優於 hard-truncation。
- `N = 32` default (row-global, 非 per-block 配額)
- Stage 2 對 N 個 candidate 補算 full-64-dim Q·K (8-bit), 需從 K buffer 隨機 gather N 個 token

**v4 Block dispatch (B / T_margin / T_overflow / M_local) 全廢除** — 丟失 row-global 性, 已被 Global Top-N 取代。

#### Stage 3: KIVI-V + P matrix 量化

- P matrix: per-row INT8 量化
- V: per-token group-wise INT4 (KIVI, Liu et al. 2024), default `G = 32`
```
For each group g of G channels:
    scale_g = (max-min)/15;  V_q = round((V-min)/scale_g) ∈ [0,15]
output = P_int8 @ V_dequant
```

---

### 1.B FFN: Activation Sparsity

#### 原理
SwiGLU: `out = W_down( SiLU(x·W_gate) ⊙ (x·W_up) )`。gate/up 雙重過濾使 `h_i = SiLU(gate_i)·up_i` 大量趨近 0。跳過低貢獻 neuron → 其 W_up[:,i]/W_down[i,:] 不載入 → 省 FFN 權重 BW。

#### Selection: gate proxy (training-free, no-up-load)
```
proxy[i] = | SiLU(INT8 gate_i) | × ||W_up[:,i]||
跳過 proxy 最低的 K_ffn 比例 neuron
```
- `||W_up[:,i]||`: 離線 L2 範數, per-model 常數, SRAM 駐留
- gate 本就要算 (SwiGLU 必要), 量化後 selection 不增 BW

#### 機制邊界 (設計依據)
廉價代理上限由 **gate 與 up 統計獨立性** 決定: 實測 `corr(|SiLU(gate)|,|up|) ≈ 0` (跨層)。真實 top neuron 約 28% 為 u 主導 (gate 看不出), 結構性損失下界。
- 砍 ~20% (retained ≥ 0.95): 接近完美選擇上界 (差 < 5 pp), PPL 損失極小
- 砍 > 30%: 需載動態 up 才選得準, 但載 up 抵消節省
- 結論: 不載動態 up 上限 ≈ 20-30% (物理邊界)

不採用 (已驗劣): `×||W_down||` (噪聲); 全精度 gate 破 50% (假省); 靜態永久死 neuron (SwiGLU 無, 死活動態); per-layer 差異化 (各層 retained 可砍率收斂)。

---

### 完整 dataflow
```
Per layer:
  [Attention]
    Per new K_i (streaming):
        K_pair[p] = max(|K_i[2p]|, |K_i[2p+1]|)
        K_runmax[p] ← max(K_runmax[p], K_pair[p])           # per-position running-max
    Per query:
        1. K-anchor: top-n_K of 當下 K_runmax
        2. Dynamic : top-(A-n_K) of |Q| pair-mag (anchor 排除)
        3. Stage 1 : active_score (mask einsum) + β·SIGN(q·sign k)
        4. Stage 2 : full_score on top-N candidate; final = where(cand, full, stage1)
        5. Softmax → P; P 量化 INT8
        6. KIVI dequant V → P @ V → attn_out
  [FFN]
        7. gate = x·W_gate                                   # dense, 必算
        8. proxy = |SiLU(INT8 gate)| × ||W_up||(SRAM)
        9. keep = top-(1-K_ffn) by proxy
       10. gather keep 的 W_up/W_down → h → ffn_out           # sparse gather
```

---

## 2. 軟體超參如何測得

### 2.1 Metric Hierarchy
| Metric | 用途 | 可信度 |
|--------|------|-------|
| Spearman ρ | 僅 active dim ratio sweep | 機制存廢預測失效, 不可作機制決策 |
| KL max | n_K sweep | 跨 model 驗證可信, primary |
| Retained magnitude | FFN 剪枝率 | 直接對應 PPL |
| End-to-end (HellaSwag/PG-19) | ground truth | 任何機制變動必須驗 |

**「最優」紀律**: 任何「最優」宣稱須附對照上界 (FFN perfect oracle、anchor 三版對照), 不可只憑「測過的最好」。

### 2.2 β (SIGN) = 1.0 (Theorem 1 + sweep 驗證)
### 2.3 KIVI group G = 32 (KIVI default, 引用不重 sweep)
### 2.4 active_dim ratio A/P = 0.375 (落在 SparQ r/d_h ∈ {25%,50%} 範圍; 新 model 三 candidate + end-to-end, 無單一事前指標)
### 2.5 Q-anchor 廢除 n_Q=0 (TinyLlama 1Q vs 0Q gap 0.01pp; Qwen KL identical; SparQ cross-model 皆無; register 保留作 fallback)
### 2.6 K-anchor 數量 n_K ∈ {1,2,3,4} per-model (n_K/P ≈ 3-5%; KL max → end-to-end)

### 2.7 K-anchor 選法: per-position streaming running-max
每 query 用「截至當前位置累積 running-max」選 top-n_K。no offline calibration。
```
Init K_runmax[L,kv_h,p]=0
Per new K_i: K_runmax[p] ← max(K_runmax[p], max(|K_i[2p]|,|K_i[2p+1]|))
Per query:   K_anchor = top-n_K of 當下 K_runmax
```
**驗證** (TinyLlama PG-19 三版對照): K_est.mean (代碼舊版) +1.70% / 純 running-max 最終值 +1.68% / **per-position 當下 max +1.63%**。三者本質相同 (差<0.1pp), 採 per-position (最優且硬體最簡: P 維 running-max 暫存器, 零額外存儲)。
**long context 優勢**: running-max 自動抓 attention sink。

### 2.8 Global Top-N 數量 N = 32 (row-global, 非 per-block)
### 2.9 FFN 剪枝率 K_ffn: per-model, default 對應 retained 門檻 τ=0.95 (TinyLlama ≈ 20%)
```
給 τ (default 0.95):
  For each layer: 用 proxy 算「砍到該層平均 retained ≥ τ 的最大剪枝率」→ per-layer K_ffn
```
- τ 單一可解釋超參; 各層 K_ffn 收斂 (差<5%) 可用全域近似; 上限見 1.B

---

## 3. Calibration / Deployment Flow

### 3.1 Pre-deployment (offline, one-time, ~30-40 min)
1. Load model + tokenizer
2. Attention active ratio (25/37.5/50%) sweep → KL max + sanity → top-2
3. n_K sweep (KL max {1,2,3,4}) → top-2
4. FFN: 算每層 ||W_up|| 範數; τ=0.95 推 per-layer K_ffn
5. Final: full HellaSwag (attn) + retained sanity (FFN)
6. Long context sanity: PG-19 window=2048
7. Write registers + load ||W_up|| 範數向量

### 3.2 Runtime (no calibration set)
- K-anchor: per-position streaming running-max
- FFN proxy: gate 算完用 SRAM 範數裁決
- n_K / N / K_ffn / G: 固定

---

## 4. 硬體實作 (parameterized RTL)

### 4.1 RTL 參數化常數
```systemverilog
parameter D_MAX        = 128;
parameter P_MAX        = 64;
parameter L_MAX        = 32;
parameter HQ_MAX       = 32;
parameter HKV_MAX      = 32;
parameter D_FF_MAX     = 16384;
parameter TOPN_MAX     = 64;
parameter ACTIVE_OPTIONS = {0.25, 0.375, 0.50};
parameter LANE_COUNT   = 6;
parameter PE_PER_LANE  = 8;
parameter KIVI_GROUP_SIZE = 32;
```

### 4.2 Runtime 配置 register
```
reg_head_dim          [7:0]
reg_num_layers        [5:0]
reg_num_q_heads       [5:0]
reg_num_kv_heads      [5:0]
reg_active_lane_count [3:0]   // 1~6 (active dim 量)
reg_n_K_anchor        [3:0]   // 1~4 (per-model)
reg_n_Q_anchor        [3:0]   // 0 (default), reserved
reg_top_n             [7:0]   // Global Top-N (default 32)
reg_BETA              [3:0]   // β shift (β=1.0 → 0)
reg_ffn_prune_K       [7:0]   // FFN 剪枝率 (~20%)
```
**移除** (Block dispatch 廢除): reg_block_size / reg_T_margin / reg_T_overflow / reg_M_local。

### 4.3 BRAM / SRAM tables
| Table | Size | Update |
|-------|------|--------|
| K_runmax | L×HKV×P_MAX×8 bit ≈ 0.7 KB | per-token max-compare (streaming) |
| W_up_norm (FFN) | **DDR 駐留, 逐層載入** | offline 算好, 不更新 |

**移除**: mean_K_drop table, Q_anchor_idx table。

**W_up_norm 不常駐 BRAM** (重要): 全層 INT8 範數 = L×d_ff×1B (TinyLlama 22×5632 ≈ 124 KB)。但 LLM 逐層執行, 第 i 層 FFN 只需該層範數 (**單層 5.6 KB INT8**)。故放 DDR, FFN 階段逐層載入, 用 Ping-Pong buffer 預取下一層藏延遲。
- 單層範數 5.6 KB vs 單層 FFN 權重 ~17-35 MB (INT8/W4) → BW 佔比 ~0.02-0.03%, 可忽略
- INT8 範數對 neuron 排序精度足夠 (範數僅決定相對大小, 非精度敏感項)
- **不佔常駐 BRAM 預算**, K/V/Q buffer 維持 ~448 KB

### 4.4 整體架構
```
KV260 FPGA
├── QK-PU
│   ├── PE Lane × LANE_COUNT (active + SIGN time-multiplex)
│   ├── Cross-bar dim selector (pair-mask → active dim)
│   ├── K-runmax update unit (per-token pair-abs-max + running-max)
│   └── Global Top-N Array (streaming top-N maintain)        ★ 取代 BS scheduler
├── Softmax (base-2 + reciprocal LUT) + P INT8 量化
├── V-PU / FFN-PU
│   ├── 16×16 systolic (INT4 V × INT8 P, KIVI per-group scale extract)
│   └── (FFN 端複用同 MAC 陣列)
├── Sparse Gather Engine                                     ★ 新增, attn+FFN 共用
│   ├── attn: Top-N token 隨機 gather (Stage 2 補算)
│   └── FFN: keep neuron 的 W_up/W_down gather
├── FFN proxy unit                                           ★ 新增
│   └── SiLU(INT8 gate) LUT × W_up_norm(DDR逐層載入) → top-(1-K)
└── Memory (K/V/Q Buffer, K_runmax [BRAM]; W_up_norm + weights [DDR4])
```

### 4.5 K-runmax update unit (per-position streaming)
取代 v4 的「K_max + mean」雙機制。純 streaming running-max:
```
Per K_i vector [D bit]:
    Step1 pair abs-max:  K_pair[p] = max(|K_i[2p]|,|K_i[2p+1]|)   // P 並行 8-bit compare
    Step2 running-max :  K_runmax[p] ← max(K_runmax[p], K_pair[p])
Per query: top-n_K of 當下 K_runmax (small top-k, n_K ≤ 4)
```
~0.5K LUT, 0 DSP, 1 cycle/K vector。無需存歷史序列。

### 4.6 Global Top-N Array (★ 取代 Block dispatch, 待詳細設計)
Row-global top-N。方案: **sorted shift register / systolic 比較陣列**。
```
N-entry sorted register (score 16-bit + index):
  每新 stage1 分數 s: if s > min_entry: 插入冒泡淘汰最小   // O(N) compare, pipeline 1 score/cycle
輸出: N candidate index → Sparse Gather Engine 補算 full_score
```
- N=32: 32-entry × (16+log2 S) bit
- ~2K LUT (待 RTL 確認), 0 DSP
- **狀態**: 架構方案確定 (streaming systolic top-N), 詳細 RTL 與時序待設計

### 4.7 Sparse Gather Engine (★ 新增, attn+FFN 共用)
attn Stage 2 與 FFN 為同一 gather pattern:
```
輸入: index list (動態 runtime)
動作: 依 index 從 DDR/BRAM 隨機讀對應 row, 對齊送 PE
```
- attn: index = Top-N token, gather K row
- FFN : index = keep neuron, gather W_up[:,keep]/W_down[keep,:]
- ~1.5K LUT, 0 DSP (兩用途分時)

### 4.8 FFN proxy unit (★ 新增)
```
proxy[i] = |SiLU(INT8 gate_i)| × W_up_norm[i]    // SiLU LUT, × DDR 載入之單層 INT8 範數
keep = top-(1-K_ffn)                              // 復用 Top-N array 或 threshold compare
```
~0.5K LUT, 0 DSP。W_up_norm 單層 5.6 KB 由 DDR 經 Ping-Pong buffer 載入 (算第 i 層時預取 i+1 層), 藏延遲。

### 4.9 PE Lane / Cross-bar / Softmax / KIVI
- PE Lane: active path (8-bit MAC + tree adder) 與 SIGN path (sign-XNOR + |Q| accumulate) time-multiplex
- Cross-bar: LANE×PE cascading 4:1 mux, pair-mask → active dim, single-cycle
- Softmax: base-2 + reciprocal LUT; P per-row INT8 量化
- KIVI: 16×16 systolic INT4×INT8, per-group scale 累加後乘 1 次

### 4.10 Layer 0 異質 mapping
物理 PE = LANE×PE = 48。Layer 0 跑滿 D: ceil(D/48) iter (D=64→2, D=128→3)。慢 2-3x 但 only 1/L layers, 整體 <10%。

### 4.11 Reconfigurability
| 參數 | 範圍 | 控制 |
|------|------|------|
| head_dim D | 64/80/96/128 | cross-bar mux (compile) |
| num_layers L | 1~L_MAX | register |
| active A | 0.25~0.50 D | active_lane_count |
| n_K | 1~4 | register |
| n_Q | 0 (default) | register (fallback) |
| Top-N | default 32 | reg_top_n |
| K_ffn | ~20% per-model | reg_ffn_prune_K |
| β | 0/0.5/1.0 | shift register |
| KIVI G | 32 | compile |

### 4.12 資源估算 (KV260, v5)
| 模組 | LUT | DSP | BRAM |
|------|-----|-----|------|
| QK-PU (PE Lane) | ~5K | LANE×PE | 0 |
| Cross-bar dim selector | ~1K | 0 | 0 |
| K-runmax update unit | ~0.5K | 0 | 0 |
| Global Top-N Array (待設計) | ~2K | 0 | 0 |
| Softmax + P 量化 | ~1.5K | 4 | 0 |
| V-PU / FFN-PU (16×16 systolic) | ~3K | 96 | 0 |
| Per-group scale extract | ~0.7K | 16 | 0 |
| Sparse Gather Engine (共用) | ~1.5K | 0 | 0 |
| FFN proxy unit | ~0.5K | 0 | 0 |
| Control + FSM | ~3K | 0 | 0 |
| K/V/Q Buffer | 0 | 0 | ~448 KB |
| K_runmax table | 0 | 0 | ~0.7 KB |
| W_up_norm (DDR, 逐層載入) | 0 | 0 | 0 常駐 (單層 5.6 KB INT8 用時載入) |
| **Total (logic)** | **~18.7K** | **~164** | **~448 KB (K/V/Q)** |

KV260 limit: 117K LUT (~16%) / 1248 DSP (13%) / 640 KB BRAM。
**Bottleneck**: BRAM (K/V buffer ~448 KB / 640), DDR4 bandwidth。W_up_norm 放 DDR 逐層載入 (見 4.3), 不佔常駐 BRAM。

### 4.13 Memory wall 應對
| 技術 | 收益 | 相容 |
|------|------|------|
| W4A8 weight (AWQ/GPTQ) | DDR4 BW 省 50% | ✓ orthogonal |
| KV cache INT4 (KIVI) | 已內建 | ✓ |
| FFN activation sparsity | FFN 權重 BW 省 ~20% | ✓ 共用 gather |

---

## 5. Simulation Caveats

### 5.1 GPU simulation 範圍
涵蓋: ✓ 機制邏輯 (active dim, SIGN, Global Top-N, per-position running-max anchor, KIVI, FFN proxy); ✓ V INT4 RTN; ✓ P INT8; ✓ FFN neuron skip。
未涵蓋: ✗ INT8 active MAC 量化 (FP32 模擬); ✗ INT8 累加溢位; ✗ softmax 8-bit fixed-point。

### 5.2 待補實驗
1. INT8 active MAC fake quantization
2. AWQ + RAD + KIVI + FFN 完整疊加
3. Cross-task (ARC, PIQA, WG)
4. LLaMA-7B 完整驗證
5. FFN proxy 跨 model (Qwen/LLaMA) retained 門檻泛化
6. Global Top-N Array RTL 詳細設計 + 時序
7. INT8 W_up_norm 排序精度確認 (跨 model 驗證時順帶)

---

## 6. Model-specific config (appendix, 數據詳見 data md)

⚠ 實驗數據, RTL 不寫死。

### 6.1 TinyLlama-1.1B (D=64, P=32, d_ff=5632)
- Attention: `n_Q=0, n_K=1, active=12 (37.5%), β=1.0, Top-N=32`, K-anchor per-position running-max
- FFN: `τ=0.95 → K_ffn ≈ 20%`
- 整合 (FP32 baseline 11.6892): RAD-only +1.70% / FFN-only(20%) +0.58% / **RAD+FFN +2.37%** (交互 +0.09 pp)

### 6.2 Qwen2.5-1.5B (D=128, P=64)
- Attention: `n_Q=0, n_K=3, active=24 (37.5%), β=1.0, Top-N=32`
- FFN: 待測

### 6.3 機制改版歷史
- **v1** RAD + PxV — 棄用 (ZipCache 論證 prob threshold 不準)
- **v2** RAD + KIVI-V (offline K-anchor, 1Q+1K)
- **v3** + Runtime K-anchor (long ctx -1.18% PPL)
- **v4** Q-anchor 廢除, n_K 約束放寬, KL max 取代 ρ (仍含 Block dispatch + mean-K SIGN, 已在 v5 修正)
- **v5 (current)**:
  - Stage 2 **Block dispatch → Global Top-N** (where(cand,full,stage1))
  - SIGN 簡化 `β·Σ q·sign(k)` (移除 mean(|K_drop|))
  - K-anchor **per-position streaming running-max** (三版對照 +1.63%, 最優且硬體最簡)
  - 移除 mean_K_drop / block FSM / margin·overflow
  - **+ FFN activation sparsity** (gate proxy, retained 門檻, 上限刻畫 g⊥u)
  - 雙稀疏整合 (交互 +0.09 pp), 共用 Sparse Gather Engine
  - W_up_norm 放 DDR 逐層載入 (INT8, 單層 5.6 KB, Ping-Pong 藏延遲), 不佔常駐 BRAM

---

## 7. Spec Discipline
- **機制決策流程**: L1 ρ filter → L2 KL max (attn)/retained (FFN) → L3 random 500 end-to-end → L4 full HellaSwag + PG-19
- **約束驗證**: 每約束須物理硬體理由, 無依據者放寬
- **Regression**: 已驗證 *_final.json 直接引用不重跑; 新機制只跑新 config + toggle, 不重寫 work cell
- **Sanity sample**: 不用 dataset 前 N (bias), 用 random/stratified 500
- **Methodology**: 機制變更須 short+long context 兩場景; runtime 機制要在長 context 展現 advantage; 「最優」宣稱須附對照上界
